import { FiltroSubscripcionesPipe } from './filtro-subscripciones.pipe';

describe('FiltroSubscripcionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSubscripcionesPipe();
    expect(pipe).toBeTruthy();
  });
});
