import { FiltroCorreoPipe } from './filtro-correo.pipe';

describe('FiltroCorreoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCorreoPipe();
    expect(pipe).toBeTruthy();
  });
});
