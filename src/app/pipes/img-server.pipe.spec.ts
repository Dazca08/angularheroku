import { ImgServerPipe } from './img-server.pipe';

describe('ImgServerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgServerPipe();
    expect(pipe).toBeTruthy();
  });
});
